# Copyright (c) 2026 RoundPenny. All rights reserved.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: ledger-service
  namespace: roundup-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ledger-service
  template:
    metadata:
      labels:
        app: ledger-service
    spec:
      containers:
      - name: ledger-service
        image: roundup/ledger-service:latest
        ports:
        - containerPort: 8087
        envFrom:
        - configMapRef:
            name: roundup-config
        env:
        - name: PORT
          value: "8087"
        - name: MIGRATIONS_DIR
          value: "/app/migrations"
        livenessProbe:
          httpGet:
            path: /v1/health
            port: 8087
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /v1/health
            port: 8087
          initialDelaySeconds: 3
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: ledger-service
  namespace: roundup-platform
spec:
  ports:
  - port: 8087
  selector:
    app: ledger-service
