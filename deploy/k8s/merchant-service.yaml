# Copyright (c) 2026 RoundPenny. All rights reserved.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: merchant-service
  namespace: roundup-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: merchant-service
  template:
    metadata:
      labels:
        app: merchant-service
    spec:
      containers:
      - name: merchant-service
        image: roundup/merchant-service:latest
        ports:
        - containerPort: 8092
        envFrom:
        - configMapRef:
            name: roundup-config
        env:
        - name: PORT
          value: "8092"
        - name: MIGRATIONS_DIR
          value: "/app/migrations"
        livenessProbe:
          httpGet:
            path: /v1/health
            port: 8092
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /v1/health
            port: 8092
          initialDelaySeconds: 3
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: merchant-service
  namespace: roundup-platform
spec:
  ports:
  - port: 8092
  selector:
    app: merchant-service
