# Copyright (c) 2026 RoundPenny. All rights reserved.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: roundup-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
      - name: notification-service
        image: roundup/notification-service:latest
        ports:
        - containerPort: 8091
        envFrom:
        - configMapRef:
            name: roundup-config
        env:
        - name: PORT
          value: "8091"
        - name: MIGRATIONS_DIR
          value: "/app/migrations"
        livenessProbe:
          httpGet:
            path: /v1/health
            port: 8091
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /v1/health
            port: 8091
          initialDelaySeconds: 3
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: roundup-platform
spec:
  ports:
  - port: 8091
  selector:
    app: notification-service
